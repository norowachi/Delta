# Services Needed

1. Docker
   > for starting the backend server & mongodb
2. Redis Instance
   > for handling the tenor API cache

# Notes to remember:

- u{id} -> user
- c{id} -> channel
- g{id} -> guild
- m{id} -> message
